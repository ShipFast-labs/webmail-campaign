PORT=8080
SPRING_PROFILES_ACTIVE=dev

# Allowed web origin for CORS (unset = allow any origin)
# CORS_ORIGIN=http://localhost:3001

# Web frontend dev origin (unset or empty = allow any origin)
CORS_ORIGIN=http://localhost:5173
